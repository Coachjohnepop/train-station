export const CHAT_VIDEO_MAX_BYTES = 20 * 1024 * 1024;
/** Short clips only — longer content should use YouTube (free storage/bandwidth). */
export const CHAT_VIDEO_MAX_DURATION_SEC = 50;
export const CHAT_IMAGE_MAX_BYTES = 5 * 1024 * 1024;